overwrite: true
schema: 'libs/user-domain/src/lib/graphql/user-domain.graphql'
hooks:
  afterAllFileWrite:
    - prettier --write
    - eslint --fix
generates:
  libs/user-domain/src/lib/generated/user.ts:
    plugins:
      - typescript-type-graphql
