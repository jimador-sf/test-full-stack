overwrite: true
hooks:
  afterAllFileWrite:
    - prettier --write
    - eslint --fix
generates:
  libs/user-domain/src/lib/generated/user-types.ts:
    schema: 'libs/user-domain/src/lib/graphql/user-domain.graphql'
    plugins:
      - typescript-type-graphql
    config:
      scalars:
        ISODateScalar: Date

