import React from 'react';
import {
  UserEditCancelButton,
  UserEditCancelButtonProps,
} from './user-edit-cancel-button';

export default {
  component: UserEditCancelButton,
  title: 'UserEditCancelButton',
};

export const primary = () => {
  /* eslint-disable-next-line */
  const props: UserEditCancelButtonProps = {};

  return <UserEditCancelButton />;
};
